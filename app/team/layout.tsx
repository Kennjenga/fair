/**
 * Public layout for team pages
 * No authentication required - accessible via voting token
 */
export default function TeamLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

