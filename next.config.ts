import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: We use Brevo REST API directly instead of the SDK to avoid Turbopack bundling issues
};

export default nextConfig;
