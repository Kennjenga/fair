'use client';

import { AppEnvironmentProvider, useAppEnvironment } from './AppEnvironmentProvider';

/**
 * Export the provider for use in layout
 */
export { AppEnvironmentProvider };

/**
 * Export the hook for use in components
 */
export { useAppEnvironment };
